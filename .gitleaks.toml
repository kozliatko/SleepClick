title = "gitleaks configuration — secret scanning"

[extend]
useDefault = true

[allowlist]
description = "Placeholders, examples and test fixtures"
paths = [
  '''\\.env\\.example$''',
  '''\\.env\\.sample$''',
  '''(^|/)tests?/''',
]
